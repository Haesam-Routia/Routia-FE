# Routia 프론트 환경변수 예시. 실제 값은 .env.local 에 두세요.
# 비워두면 상대경로(/api/...) 로 요청 → Vite dev proxy(아래 참고) 또는 동일 오리진 사용.
VITE_API_BASE_URL=

# 백엔드가 http://localhost:8080 에 뜬다면 아래처럼 프록시가 처리합니다.
# vite.config.ts 의 server.proxy 대상(VITE_API_PROXY_TARGET)을 조정하세요.
VITE_API_PROXY_TARGET=http://localhost:8080
